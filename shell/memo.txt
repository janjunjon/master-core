sudo apt update
sudo apt install gcc
sudo apt install gfortran
sudo apt install g++
sudo apt install libcurl4-openssl-dev
sudo apt install m4
sudo apt install mlocate
sudo apt install make

locate pthread.h
locate libpthread.so

sh shell/*.sh(zlib,szip,jpeg,hdf5)

# for netCDF4
export FC=gfortran
export CC=gcc

# hdf5 path
export CPATH=$CPATH:/usr/local/hdf5/include
export LIBRARY_PATH=$LIBRARY_PATH:/usr/local/hdf5/lib

# netCDF4
export NCDIR=/usr/local/netcdf-c-4.7.4
export CPPFLAGS="-I/usr/local/szip/include -I/usr/local/hdf5-1.10.5/include"
export LDFLAGS="-L/usr/local/szip/lib -L/usr/local/hdf5-1.10.5/lib"

cd
. .profile

sudo su
cd /home/ubuntu/netcdf-c-4.7.4
sudo ./configure --prefix=${NCDIR} --enable-netcdf-4 --enable-shared --enable-dap --disable-dap-remote-tests --with-curl=/usr/include/x86_64-linux-gnu/curl
sudo make
sudo make install


# OpenMPI
sudo su
apt install openmpi-bin

# LAPACK
sudo su
apt install libatlas3-base libatlas-base-dev

# grads, netCDF4, SCALE
sudo su
apt install grads
apt install libnetcdf-dev
vim /etc/profile
  export SCALE_SYS="Linux64-gnu-ompi"
nc-config --version
exit
wget https://scale.riken.jp/archives/scale-5.4.4.tar.gz -P ~/
sudo tar zxvf ~/scale-5.4.4.tar.gz -C ~/
sudo su
cd /home/ubuntu/scale-5.4.4/scale-rm/src